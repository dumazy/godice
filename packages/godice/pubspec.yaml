name: godice
description: >-
  Pure Dart library for GoDice smart dice: BLE protocol constants, command
  builders, notification parsing, shell-aware roll interpretation and a
  transport-agnostic GoDice client.
version: 0.1.0
repository: https://github.com/dumazy/godice
issue_tracker: https://github.com/dumazy/godice/issues
topics:
  - godice
  - dice
  - bluetooth
  - ble
  - tabletop
resolution: workspace

environment:
  sdk: ^3.8.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
